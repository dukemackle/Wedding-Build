// Shared preset meal choices, used by both the public RSVP form and the
// couple's manual guest form, so catering headcounts can actually be
// tallied by choice instead of parsing arbitrary free text.
export const MEAL_OPTIONS = ["Chicken", "Fish", "Vegetarian", "Vegan", "Kids Meal", "Other"] as const;
